coordinator=false
http-server.http.port=8080
discovery.uri=http://trino-cord:8080
# jmx.rmiregistry.port=9080 - these don't need to be changed unless a different port is needed
# jmx.rmiserver.port=9181